<p style="color: green"><%= notice %></p>

<h1>Accounts</h1>

<table id="accounts" class="table table-striped" data-controller="dataTables">
  <thead>
    <tr>
      <th>Account Number</th>
      <th>Alias</th>
      <th>Edit</th>
      <th>Delete</th>
    </tr>
  </thead>
  <tbody>
    <% @accounts.each do |account| %>
    <%= render account %>
  <% end %>
  </tbody>
</table>

<%= link_to "New account", new_account_path %>