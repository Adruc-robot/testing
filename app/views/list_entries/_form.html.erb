<%= form_with(model: [@list, list_entry]) do |form| %>
  <% if list_entry.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(list_entry.errors.count, "error") %> prohibited this list_entry from being saved:</h2>

      <ul>
        <% list_entry.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.number_field :list_id, id: :list_entry_list_id, value: @list.id, type: :hidden %>
  </div>

  <div class="form-group">
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= form.label :order_number, style: "display: block" %>
    <%= form.number_field :order_number, class: "form-control" %>
  </div>

  <div>
    <%= form.submit class: "btn btn-primary"  %>
  </div>
<% end %>
